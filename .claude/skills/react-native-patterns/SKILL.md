---
name: react-native-patterns
description: React Native and Expo implementation standards for performance, lists, rendering, touch targets, and mobile-safe UI behaviour.
allowed-tools: Read, Grep
---

# React Native Patterns

- Use virtualization for large lists
- Avoid sync-heavy work on the JS thread
- Keep touch targets accessible
- Measure performance before optimizing
- Respect safe area, loading, error, and empty states
