---
name: typescript-strict
description: Strict TypeScript enforcement for safe boundaries, explicit state, exhaustive branching, and runtime validation.
allowed-tools: Read, Grep
---

# TypeScript Strict

- No implicit `any`
- Prefer explicit return types for exported functions
- Model unknown external input explicitly
- Use exhaustive handling for discriminated unions
- Avoid leaking transport types into domain or UI
