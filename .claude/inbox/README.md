# Inbox

Drop raw `*.summary.md` product briefs here before running `/business-to-sdd`.

Rules:

- one summary per feature
- keep user value, flow, data, and out-of-scope explicit
- summaries are disposable after SDD creation
- `.claude/sdd/` is the source of truth after conversion
