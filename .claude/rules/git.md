---
paths:
  - ".gitignore"
  - ".github/**/*.yml"
  - ".github/**/*.yaml"
---

# Git Rules

- Never auto-commit or auto-push
- Never push directly to `main`
- Use Conventional Commits
- Keep workflow changes auditable and intentional
- Treat AI system structure changes as architecture changes
