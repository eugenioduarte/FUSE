---
paths:
  - "src/**/*.ts"
  - "src/**/*.tsx"
---

# TypeScript Rules

- No implicit `any`
- Type `useState` when inference is ambiguous
- Prefer exhaustive `switch` handling
- Avoid unsafe assertions and silent escapes
- Validate external data at runtime when the boundary is untrusted
