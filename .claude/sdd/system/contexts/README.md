# Contexts

This folder stores long-lived domain memory.

Recommended bootstrapping order:

1. `auth`
2. `dashboard`

Each domain should start from the templates in `.claude/sdd/system/templates/context/`.
