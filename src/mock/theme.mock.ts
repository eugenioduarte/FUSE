import { Colors, border, spacings, typography } from '../constants/theme'
import { ThemeType } from '../types/theme.type'

export const themeMock: ThemeType = {
  colors: Colors.light,
  spacings,
  typography,
  border,
}
