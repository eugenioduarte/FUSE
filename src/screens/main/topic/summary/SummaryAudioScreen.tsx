import React from 'react'
import { Text, View } from 'react-native'

// Audio screen removed. This placeholder keeps imports safe if any
// references remain temporarily; the UI should no longer navigate here.
const SummaryAudioScreen: React.FC = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Audio feature removed</Text>
  </View>
)

export default SummaryAudioScreen
