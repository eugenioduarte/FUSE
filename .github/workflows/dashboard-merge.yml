name: Dashboard Publish

on:
  push:
    branches: [main]

concurrency:
  group: dashboard-merge
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  publish:
    name: Generate and deploy dashboard
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: https://eugenioduarte.github.io/FUSE/demonstration-orchestration.html

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate dashboard
        run: bash .ai/scripts/generate-dashboard.sh

      - name: Commit generated HTML
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/demonstration-orchestration.html
          git commit -m "chore: update dashboard [skip ci]" || true
          git push || true

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
